# Tae-Codex


This repository contains simple Python scripts.

## progress.py

Use `progress.py` to calculate the percentage of project completion.

```bash
python3 progress.py TOTAL_TASKS COMPLETED_TASKS
```

The script validates input and prints the completion percentage.
